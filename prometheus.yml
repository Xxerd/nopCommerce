global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Metrics pushed from nopCommerce via OTel Collector
  - job_name: nopcommerce
    static_configs:
      - targets: ['otel-collector:8889']
